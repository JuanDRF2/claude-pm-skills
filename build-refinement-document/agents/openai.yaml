interface:
  display_name: "Build Refinement Document"
  short_description: "Generate a clear Word refinement document"
  default_prompt: "Use $build-refinement-document to turn this approved Markdown refinement package into a readable, validated Word document."
