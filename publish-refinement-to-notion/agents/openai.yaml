interface:
  display_name: "Publish Refinement to Notion"
  short_description: "Publish a collaborative view and 1:1 Markdown mirror"
  default_prompt: "Use $publish-refinement-to-notion to publish this approved package as a collaborative Notion refinement view plus a synchronized 1:1 Markdown mirror."
